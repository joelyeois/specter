# API reference

mkdocstrings builds these pages from the docstrings in `src/specter/`. Each
page below documents one subpackage's public interface (what it re-exports
through its `__init__.py`), plus a page for the top-level standalone modules.

For the `specter` command rather than the Python interface, see
[CLI](cli/index.md).

[Pipeline overview](../concepts/pipeline-overview.md) shows how the pieces fit together.
